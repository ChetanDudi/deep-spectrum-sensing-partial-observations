# Configs

This folder stores configuration files used by the reproducible training pipeline.

## Files

- `train_config.json`
  Default baseline training configuration for `training/train.py`.

You can duplicate this file to create experiment-specific configurations without changing the project defaults.
