# Models

This folder contains the neural network definitions used by the training pipeline.

## Files

- `recurrent.py`
  Recurrent baselines for band-occupancy prediction, including LSTM, Bi-LSTM, GRU, and vanilla RNN variants.

These models are baseline scaffolding aligned with the project report and dataset format.
