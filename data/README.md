# Data

This folder groups project datasets and artifacts by purpose.

## Layout

- `sample_results/`
  Example PSD logs and `.pth` artifacts captured from the SDR pipeline and kept in the repository as reference data.

- `runtime_captures/`
  Default destination for new receiver capture outputs. Ignored by Git.

- `generated_datasets/`
  Default destination for synthesized training datasets. Ignored by Git.
